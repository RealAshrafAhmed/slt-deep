# markov-chain

Discrete Markov chain utilities for sequence generation experiments.

Provides `gamblers_ruin` (transition matrix constructor) and `generate_sequences`
(reproducible sequence sampler) used across the markov-transformer experiment notebooks.
